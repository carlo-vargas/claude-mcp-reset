# claude-mcp-reset
A script that launches Claude Code CLI with all available MCP disabled.

## Motivation
I want to start a new Claude Code CLI session with all MCP disabled, so my context is not loaded, then I want to manually enable the MCP I really need using "/mcp" command.
